# BTReDS SQUAD FLASH PEAK - Cloudflare Google OAuth

Cloudflare Workers Static Assets deployment.

Build command: `npx wrangler deploy`

Assets directory: `./public`

Google OAuth uses Supabase Auth. Keep the Google Client Secret only in Supabase; never put it in this repository.
